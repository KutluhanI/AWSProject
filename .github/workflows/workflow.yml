name: Upload Website

on:
  push:
    branches:
    - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: jakejarvis/s3-sync-action@master
      with:
        args: --acl public-read --follow-symlinks --delete
      env:
        AWS_S3_BUCKET: ${{ ilikeallbuckets }}
        AWS_ACCESS_KEY_ID: ${{ ASIA47OG4K2AMRIMKTN5 }}
        AWS_SECRET_ACCESS_KEY: ${{ dbRw/XH2+E+4wjj6ghzm3KpP8qfZVx08X5qV2IST }}
        AWS_REGION: 'us-east-1'
        SOURCE_DIR: 'public'      
